# ACT handles each camera separately. No additional spatial transform.
# Match dataset-side transforms explicitly if training used any.
# Only additional Cyclo preprocessing is configured here; API packing stays in Python.
preprocessing: identity
