# FastWAM owns per-view resize and concatenation. Its input_features may
# describe post-resize dimensions, not the raw dataset images.
# Only additional Cyclo preprocessing is configured here; API packing stays in Python.
preprocessing: identity
