# WALL-X: modeling_wall_x.preprocess_inputs performs PIL/Qwen image preparation
# per camera. Keep the original RGB dimensions before that model-owned transform.
# The pinned model's 20-dimensional state/action limit is checked separately on LOAD.
# Only additional Cyclo preprocessing is configured here; API packing stays in Python.
preprocessing: identity
